{
  "name": "cluster-slot-local-math",
  "version": "0.1.0",
  "private": true,
  "description": "Local 5x6 cluster math + sample books for Storybook (Stake web-sdk integration)",
  "type": "module",
  "scripts": {
    "check": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
